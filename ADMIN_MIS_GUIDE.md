# ChinaMedCare Admin MIS 管理系统

独立的医院管理信息系统（MIS）。

---

## 功能特性

- 🔐 管理员登录/登出
- 🏥 医院列表管理
- ➕ 创建新医院
- ✏️ 编辑医院信息
- 🗑️ 删除医院
- 🔍 搜索和筛选
- 📄 分页显示

---

## 快速开始

### 1. 安装依赖

```bash
cd frontend
npm install
```

### 2. 启动开发服务器

```bash
# 确保后端在运行（Docker）
cd /mnt/d/Code/MedicalCare
docker-compose -f docker-compose.dev.yml up -d

# 启动前端开发服务器
cd frontend
npm run dev
```

### 3. 访问管理系统

在浏览器中打开：**http://localhost:3000/admin**

---

## 默认登录账户

| 用户名 | 密码 |
|--------|------|
| admin | admin123 |

---

## 文件结构

```
frontend/src/
├── api/
│   ├── index.js          # 公开 API
│   └── admin.js          # 管理后台 API
├── stores/
│   └── auth.js           # Pinia 认证状态管理
├── views/
│   └── admin/
│       ├── Login.vue     # 登录页
│       ├── Layout.vue    # 管理后台布局
│       └── HospitalList.vue  # 医院管理页
└── router/
    └── index.js          # 路由配置（含认证守卫）
```

---

## 路由说明

| 路径 | 说明 |
|------|------|
| `/admin/login` | 管理员登录页 |
| `/admin` | 管理后台（重定向到医院列表） |
| `/admin/hospitals` | 医院管理页 |

---

## 使用说明

### 登录系统

1. 访问 `/admin/login`
2. 输入用户名：`admin`
3. 输入密码：`admin123`
4. 点击 "Login"

### 医院管理

#### 查看医院列表
- 登录后自动进入医院列表
- 支持分页显示（10/20/50/100 条/页）

#### 搜索医院
- 在搜索框输入关键词
- 点击 "Search" 或按回车

#### 添加医院
1. 点击 "Add Hospital" 按钮
2. 填写医院信息：
   - Hospital Name（医院名称）
   - Location（位置）
   - Rating（评分 0-5）
   - Image URL（图片链接）
   - Departments（科室，可多选或创建）
   - Languages（语言，可多选或创建）
3. 点击 "Save"

#### 编辑医院
1. 在医院列表中找到要编辑的医院
2. 点击 "Edit" 链接
3. 修改信息
4. 点击 "Save"

#### 删除医院
1. 在医院列表中找到要删除的医院
2. 点击 "Delete" 链接
3. 在确认对话框中点击 "Delete"

#### 退出登录
1. 点击右上角用户名
2. 在下拉菜单中选择 "Logout"
3. 确认退出

---

## API 集成

管理后台使用以下 API：

### 认证
- `POST /api/admin/login` - 管理员登录
- `POST /api/admin/logout` - 退出登录

### 医院管理
- `GET /api/admin/hospitals` - 获取医院列表（分页）
- `POST /api/admin/hospitals` - 创建医院
- `PUT /api/admin/hospitals/{id}` - 更新医院
- `DELETE /api/admin/hospitals/{id}` - 删除医院

---

## 技术栈

- **Vue 3** - 前端框架
- **Pinia** - 状态管理
- **Vue Router** - 路由管理
- **Element Plus** - UI 组件库
- **Axios** - HTTP 客户端

---

## 开发提示

### 添加新页面
1. 在 `frontend/src/views/admin/` 下创建新页面
2. 在 `frontend/src/router/index.js` 中添加路由
3. 在 `Layout.vue` 的侧边栏菜单中添加链接

### 添加新 API
1. 在 `frontend/src/api/admin.js` 中添加 API 函数
2. 在组件中导入并使用

---

## 安全说明

- 登录 Token 存储在 localStorage 中
- 所有管理 API 请求都会自动添加 Authorization header
- 401 响应会自动跳转到登录页
- 路由守卫会保护需要认证的页面
